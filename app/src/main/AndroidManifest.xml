<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.janej.andriodlabs">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application

        android:label="@string/app_name"

        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ListItemsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StartActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".ChatWindowActivity" />
        <activity android:name=".WeatherForecastActivity" />
        <activity android:name=".MessageDetailsActivity" />
        <activity
            android:name=".TestToolbar"
            android:label="@string/title_activity_test_toolbar"></activity>
    </application>

</manifest>